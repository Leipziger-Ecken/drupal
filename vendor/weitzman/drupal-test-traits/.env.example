# This file is for customizing the Docker environment which is used to develop
# DTT. If you are just using DTT for tests, you may ignore this file
# and docker-compose.yml.

# Change if you want this site to be accessible at a different port.
# WEB_PORT=81

# XDebug defaults to Off in the drupal container (IDEKEY=PHPSTORM, Port 9000).
#
# Uncomment to enable XDebug.
# XDEBUG_ENABLE=1
#
# Specify a remote_host (only needed for Windows & Linux hosts).
# XDEBUG_CONFIG=remote_host=docker.for.mac.localhost

# Change if you want chromedriver to be accessible at a different port on the host.
# CHROMEDRIVER_PORT=9222

# Change if you want selenium2driver to be accessible at a different port on the host.
# SELENIUM2DRIVER_PORT=4444

# Specify a temporary directory for storing debug images and html documents.
# These artifacts get copied to /sites/simpletest/browser_output by BrowserTestBase
# BROWSERTEST_OUTPUT_DIRECTORY=

# Silence deprecation notices when using HtmlOutputPrinter.
# SYMFONY_DEPRECATIONS_HELPER=disabled

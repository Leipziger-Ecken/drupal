{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{% set layout = content.field_cta_layout[0]['#markup'] %}
{% block paragraph %}
{% if layout == 'md' %}
<section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
<!-- We've used 3xl here, but feel free to try other max-widths based on your needs -->
  <div class="bg-primary-100">
    <div class="max-w-2xl mx-auto text-center py-16 px-4 sm:py-20 sm:px-6 lg:px-8">
      {% if content.field_heading[0] %}
      <h2 class="text-3xl font-extrabold sm:text-4xl">
        <span class="block">{{ content.field_heading }}</span>
      </h2>
      {% endif %}
      <div class="mt-4 text-lg leading-6 prose">
        {{ content.field_body }}
      </div>
      <div class="mt-5 max-w-md mx-auto items-end sm:flex sm:justify-center md:mt-8">
        {% if content.field_primary_button[0] %}
        {{ include('@leipzigerEckenWebbuilder/partials/components/button.html.twig', {
          href: content.field_primary_button[0]['#url'],
          class: 'mt-8 w-full h-auto sm:w-auto',
          label: content.field_primary_button[0]['#title'],
        }) }}
        {% endif %}

        {% if content.field_secondary_button[0] %}
        {{ include('@leipzigerEckenWebbuilder/partials/components/button.html.twig', {
          type: 'secondary',
          href: content.field_secondary_button[0]['#url'],
          class: 'mt-3 w-full sm:mt-0 sm:ml-3',
          label: content.field_secondary_button[0]['#title'],
        }) }}
        {% endif %}
      </div>
    </div>
  </div>
</section>
{% elseif layout == 'lg' %}
<section class="relative bg-gray-50 overflow-hidden">
  <div class="relative pt-6 pb-16 sm:pb-24">
    <main class="mt-16 mx-auto max-w-7xl px-4 sm:mt-24">
      <div class="text-center">
        {% if content.field_heading[0] %}
        <h1 class="text-4xl tracking-tight font-extrabold text-gray-900 sm:text-5xl md:text-6xl">
          <span class="block xl:inline">{{ content.field_heading }}</span>
        </h1>
        {% endif %}
        <div class="mt-3 max-w-md mx-auto text-base text-gray-500 sm:text-lg md:mt-5 md:text-xl md:max-w-3xl prose">
          {{ content.field_body }}
        </div>
        <div class="mt-5 max-w-md mx-auto items-end sm:flex sm:justify-center md:mt-8">
          {% if content.field_primary_button[0] %}
          {{ include('@leipzigerEckenWebbuilder/partials/components/button.html.twig', {
            type: 'primary',
            size: 'lg',
            href: content.field_primary_button[0]['#url'],
            class: 'mt-8 w-full shadow sm:w-auto',
            label: content.field_primary_button[0]['#title'],
          }) }}
          {% endif %}

          {% if content.field_secondary_button[0] %}
          {{ include('@leipzigerEckenWebbuilder/partials/components/button.html.twig', {
            type: 'secondary',
            size: 'lg',
            href: content.field_primary_button[0]['#url'],
            class: 'mt-3 w-full shadow sm:mt-0 sm:ml-3',
            label: content.field_secondary_button[0]['#title'],
          }) }}
          {% endif %}
        </div>
      </div>
    </main>
  </div>
</section>
{% endif %}
{% endblock paragraph %}

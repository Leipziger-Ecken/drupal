<section class="container mx-auto px-4 sm:px-6 lg:px-8">
  <div class="bg-white pb-20 px-4 sm:px-6 lg:pb-28 lg:px-8">
    <div class="relative max-w-lg mx-auto divide-y-2 divide-gray-200 lg:max-w-7xl">
      <div>
        <h2 class="text-3xl tracking-tight font-extrabold text-gray-900 sm:text-4xl">
          Aktuelles
        </h2>
        <div class="mt-3 sm:mt-4 lg:grid lg:grid-cols-2 lg:gap-5 lg:items-center">
          <p class="text-xl text-gray-500">
            Neuigkeit aus dem Bülowviertel.
          </p>

          {% if filters %}
          {{ exposed }}
          {% endif %}
          {#
          <form class="mt-6 flex flex-col sm:flex-row lg:mt-0 lg:justify-end">
            <div>
              <label for="search" class="sr-only">Suchbegriff</label>
              <input id="search" name="search" type="text" required class="appearance-none w-full px-4 py-2 border border-gray-300 text-base rounded-md text-gray-900 bg-white placeholder-gray-500 focus:outline-none focus:ring-primary-500 focus:border-primary-500 lg:max-w-xs" placeholder="Suchbegriff eingeben">
            </div>
            <div class="mt-2 flex-shrink-0 w-full flex rounded-md shadow-sm sm:mt-0 sm:ml-3 sm:w-auto sm:inline-flex">
              <button type="button" class="w-full px-4 py-2 border border-transparent rounded-md flex items-center justify-center text-base font-medium text-white bg-gray-800 hover:bg-gray-900 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-900 sm:w-auto sm:inline-flex">
                Suchen
              </button>
            </div>
          </form>
          #}
        </div>
      </div>

      {% if rows -%}

      {# Featured articles #}
      {% for index,row in rows[0]['#rows'] %}
      {% if row['#node'].sticky[0].value == '1' %}
        {{ include('@leipzigerEckenWebbuilder/partials/listings/_views_listing--featured-teaser.html.twig', {content: row['#node'], index: index}) }}
      {% endif %}
      {% endfor %}

      <div class="mt-6 pt-10 grid gap-16 lg:grid-cols-2 lg:gap-x-5 lg:gap-y-12">
        {# regular articles #}
        {% for index,row in rows[0]['#rows'] %}
        {% if row['#node'].sticky[0].value == '0' %}
          {{ include('@leipzigerEckenWebbuilder/partials/listings/_views_listing--teaser.html.twig', {content: row['#node'], index: index}) }}
        {% endif %}
        {% endfor %}
      </div>

      {% elseif empty -%}
        {{ empty }}
      {% endif %}

      {{ pager }}
    </div>
  </div>
</section>

<section
  class="bg-gray-50"
  data-role="paragraph"
>
  <div class="{{ webbuilder_classes.container.lg }} py-24">
    {% if content.field_heading[0] or content.field_body[0] %}
    <div class="max-w-lg mx-auto mb-16 text-center">
      {% if content.field_heading[0] %}
      <h2 class="mb-3 {{ webbuilder_classes.text.h2 }}">
        {{ content.field_heading }}
      </h2>
      {% endif %}
      {% if content.field_body[0] %}
      <div class="{{ webbuilder_classes.text.prose }}">
        {{ content.field_body }}
      </div>
      {% endif %}
    </div>
    {% endif %}

    <div class="w-full h-screen m-auto">
      {{ content.field_map }}
    </div>
  </div>
</section>

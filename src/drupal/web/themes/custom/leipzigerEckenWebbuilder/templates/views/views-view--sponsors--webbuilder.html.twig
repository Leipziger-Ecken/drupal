{{ title_prefix }}
{{ title }}
{{ title_suffix }}

{# regular articles #}
{% for index,row in rows[0]['#rows'] %}
<div class="col-span-1 flex justify-center py-8 px-8 bg-gray-50">
  {{ include('@leipzigerEckenWebbuilder/views/_views_listing--teaser.html.twig', {content: row['#node'], index: index}) }}
</div>
{% endfor %}
